@model PharmaCite_Pharmacy_Managment_System.Models.MedicinesSold

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>MedicinesSold</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SoldId" class="control-label"></label>
                <input asp-for="SoldId" class="form-control" />
                <span asp-validation-for="SoldId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SoldBy" class="control-label"></label>
                <select asp-for="SoldBy" class ="form-control" asp-items="ViewBag.SoldBy"></select>
            </div>
            <div class="form-group">
                <label asp-for="SoldMedicine" class="control-label"></label>
                <select asp-for="SoldMedicine" class ="form-control" asp-items="ViewBag.SoldMedicine"></select>
            </div>
            <div class="form-group">
                <label asp-for="SoldTo" class="control-label"></label>
                <select asp-for="SoldTo" class ="form-control" asp-items="ViewBag.SoldTo"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
